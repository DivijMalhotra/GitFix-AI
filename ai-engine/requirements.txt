fastapi==0.111.0
uvicorn[standard]==0.30.1
python-dotenv==1.0.1
pydantic==2.7.4
httpx==0.27.2

# LLM
google-generativeai==0.8.3
openai==1.66.3

# Embeddings (ONNX-based, no PyTorch)
# NOTE: fastembed is installed via Dockerfile with --no-deps to skip heavy
# optional extras (PyStemmer/mmh3/snowballstemmer — sparse models only).
# Its core runtime deps (onnxruntime, huggingface-hub, tokenizers, tqdm,
# pillow, loguru) are pre-installed in the Dockerfile before fastembed.
# numpy is intentionally NOT pinned here — onnxruntime resolves the ABI-
# compatible version (>=2.x) and pinning 1.26.4 breaks onnxruntime at runtime.

# Code processing
gitpython==3.1.43

# Utils
aiofiles==23.2.1
tenacity==8.3.0
